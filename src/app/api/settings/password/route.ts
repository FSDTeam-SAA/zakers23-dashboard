import{NextRequest,NextResponse}from"next/server";export async function PATCH(r:NextRequest){const b=process.env.BACKEND_API_URL??process.env.NEXT_PUBLIC_API_URL,t=r.cookies.get("zakers_admin_session")?.value;if(!b)return NextResponse.json({success:false,message:"BACKEND_API_URL is not configured"},{status:500});const x=await fetch(`${b}/users/change-password`,{method:"PATCH",headers:{"Content-Type":"application/json",...(t?{Authorization:`Bearer ${t}`}:{})},body:await r.text()});return NextResponse.json(await x.json(),{status:x.status})}
