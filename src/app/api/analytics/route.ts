import { NextRequest, NextResponse } from "next/server";
export async function GET(request: NextRequest) { const base = process.env.BACKEND_API_URL ?? process.env.NEXT_PUBLIC_API_URL; const token = request.cookies.get("zakers_admin_session")?.value; if (!base) return NextResponse.json({ success: false, message: "BACKEND_API_URL is not configured" }, { status: 500 }); const response = await fetch(`${base}/analytics`, { headers: token ? { Authorization: `Bearer ${token}` } : {}, cache: "no-store" }); return NextResponse.json(await response.json(), { status: response.status }); }
